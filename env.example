# Zendesk
ZENDESK_ENV=sandbox                        # "sandbox" | "production" — never flip to production without sign-off
ZD_EMAIL=service-account@7shifts.com
ZD_API_TOKEN=                              # Zendesk API token — never commit real value
ZD_LINEAR_FIELD_ID=                        # Numeric ID of the "Linear Issue" custom field in Zendesk

# Aether / DB
DB_CONNECTION_STRING=                      # Postgres connection string for release_feedback_registry
